<% if user_signed_in? %>
  logged in as <%= current_user.name %>
  <%#= link_to('Logout', destroy_user_session_path, method: :delete)%>
<% end %>
<h1><%= @organization.name %></h1>

<%= link_to 'View Shift', dashboard_shift_edit_path(params[:id].to_i) %>
<%= link_to 'edit', dashboard_edit_organization_path(@organization.id) %>
<%= form_for :leave_organization do |f| %>
 <%= f.hidden_field :id, value: current_user.id %>
 <%= f.submit "leave"%>
<% end %>

